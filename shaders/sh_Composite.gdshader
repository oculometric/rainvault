shader_type canvas_item;

uniform sampler2D game_texture : source_color;
uniform sampler2D ui_texture : source_color;

void vertex()
{
}

void fragment()
{
	//COLOR = texture(tex, UV);//vec4(1, 0, 0, 0.1);
}
